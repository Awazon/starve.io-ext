昔はそうではなかったようですが、私がStarve.ioの解析を開始した時点(2019年3月頃)では既にコードに難読化がかけられており、解読が困難でした

＞昔はそうではなかったようですが
2年程前の話ですが、Starve.io Extensionsという拡張機能が存在しており、コードを見る限り変数、関数等に難読化がかけられていなかったようです(その変数や関数がなんの意味を持つのか大体名前で分かる)
今はアップデート毎に変数名がランダムな文字になっています
私はStarve.ioに結構ハマっており、その値がなんなのかを大体察することができたので多少の解読が可能でした
値を見て大まかな予想を立て、それに基づきテストを繰り返して解読を行いました

以下私のメモ(面倒だったので少ししかメモってません)

p
・プレイヤー系の情報が格納されている
p.Ju = 動的オブジェクト？
p.Ju[60] = 狼
    [70] = 種(麦？)


m(中身は関数li)
・トークンやカメラ座標が格納されている
m.H9 = 生きている場合true、falseにすると操作不能になる

m.lG.Eu = ライフゲージを1/100したもの
m.lG.Up = フードゲージを1/100したもの
m.lG.PT = 水ゲージを1/100したもの
m.lG.c = コールドゲージを1/100したもの
m.lG.bV = コールドゲージ(暑さの方)を1/100したもの(反転されているので、20%の暑さだと0.8になる)

もちろん、今は変数の名前が変更されています。が、オブジェクトの構造は変わらないので一度解読に成功すればその後もなんとなく分かります
例外として、名前が変更されない変数や関数が存在します
それらを用いてある程度変数の自動検出が可能です
